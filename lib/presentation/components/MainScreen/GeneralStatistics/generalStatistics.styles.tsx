import {StyleSheet} from 'react-native';

export const GeneralStatisticsStyles = StyleSheet.create({
  container: {
    width: '100%',
  },

  datesList: {
    width: '100%',
    marginTop: 20,
  },
});
